import Root from './button.svelte';
import {
	buttonVariants,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
} from './buttonVariants.js';

export {
	Root,
	Root as Button,
	buttonVariants,
	type ButtonProps as Props,
	type ButtonProps,
	type ButtonSize,
	type ButtonVariant
};
